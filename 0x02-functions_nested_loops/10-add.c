#include "main.h"
/**
 * add - add tow integers and return ther result
 *@x: number one to be added
 *@y: number two to be added
 * Return: result of the addition
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
