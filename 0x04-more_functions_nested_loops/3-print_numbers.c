#include "main.h"
#include <stdio.h>
/**
 * print_numbers - print 0 to 9 followed by a new line
 * Return; the number 0 to 9
 */

void print_numbers(void)
{
	int num;

	for (num = i 48; num <= 57; num++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
